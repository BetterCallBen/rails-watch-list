<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Submit bookmark", class: "btn btn-submit" %>
<% end %>

<%= link_to 'Back', list_path(@list), class:"btn btn-back" %>
